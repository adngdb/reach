// Namespace
REACH = window.REACH || {};

REACH.config = {
    pointsToWin: 3,

    width: 10,  // number of cells
    height: 10,  // number of cells
    cell: {
        width: 101,  // in pixels
        height: 171,  // in pixels
        offsetTopY: 50,  // in pixels, offset before each cell on Y
        offsetBottomY: 38  // in pixels, offset after each cell on Y
    }
};
